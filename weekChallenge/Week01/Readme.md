# Week 01 - Sloppy RPG

> KTB 위클리 챌린지 1주차 — CLI 기반 RPG 게임 접속 시스템

---

## 목표

커맨드라인에서 동작하는 RPG 게임 인증 시스템 구현

- 계정 생성 / 로그인 / 계정 삭제
- AES-CBC 암호화를 통한 비밀번호 보안 처리
- JSON 파일 기반 유저 데이터 영속성

---

## 프로젝트 구조

```
Week01/
├── sloppygame.py          # 메인 진입점 (CLI 라우팅)
├── utils/
│   ├── loginUtils.py      # 로그인 인자 파싱 및 검증
│   └── menuUtils.py       # 로그인 후 메인 메뉴 처리
├── user/
│   ├── utils.py           # 유저 CRUD, AES 암호화/복호화
│   └── userLoginData.json # 유저 데이터 저장소
└── requirements.txt
```

---

## 설치 및 실행

**의존성 설치**

```bash
pip install pycryptodome
```

**계정 생성**

```bash
python3 sloppygame.py create --id <아이디> --pwd <비밀번호>
```

**로그인**

```bash
python3 sloppygame.py login --id <아이디> --pwd <비밀번호>
```

---

## 주요 기능

### 인증 흐름

| 상황 | 결과 |
|---|---|
| ID 없음 | `해당 아이디가 존재하지 않습니다` 출력 |
| ID 있음 + 비밀번호 불일치 | `비밀번호가 옳지 않습니다` 출력 |
| ID 있음 + 비밀번호 일치 | 로그인 성공 → 메인 메뉴 진입 |
| 이미 존재하는 ID로 생성 시도 | `이미 존재하는 ID입니다` 출력 |

### 메인 메뉴 (로그인 후)

```
1. 게임 접속    → 로딩 애니메이션 후 서버 준비 중 안내
2. 계정 삭제    → 확인 후 JSON에서 유저 데이터 제거
3. 게임 종료    → 종료 메시지 출력
```

### 비밀번호 보안

- `pycryptodome`의 AES-CBC 방식으로 암호화
- IV + 암호문을 Base64 인코딩하여 JSON에 저장
- 평문 비밀번호는 어디에도 저장되지 않음

---

## 데이터 형식

`user/userLoginData.json`

```json
{
  "Users": {
    "userId": "<base64(IV)><base64(암호문)>"
  }
}
```

---

## 사용 예시

```
##########################################
#        Welcome To Sloppy RPG...        #
##########################################

USER ID : steve
USER PWD : *********

--------------
로그인 성공!!!
--------------

---------------------------------
- Welcome, steve
---------------------------------

1. 게임 접속
2. 계정 삭제
3. 게임 종료

메뉴를 선택해주세요:
```

---

## 회고

<details>
<summary><b>기획 단계</b></summary>
오랜만에 Python으로 코딩을 하며 CLI라는 프로그램을 작성해야한다고 들었다.
Alex강사님과 얘기 후 command가 이어져도 된다는 말을 듣고 그럼 간단한 RPG게임 시스템을 만들어 보면
어떨까 라는 생각이 들었다. 
그래서 RPG 게임이 되는 것처럼 우선 로그인 시스템을 만들어 사용자가 생성/접속/삭제 까지 되는 기능을 만들어 보고자 한다.

문제: 너무 방대해지지 않을지? 캐릭터 정보는 그냥 .json에 숨기게?
해결: 너무 스토리적이나 그런 것이 아닌 기능에 충실하고자 하며 간단히 동작하도록 우선 만들예정. 그리고 argparse를 사용해보기 위해서 .json에 넣고 
암호화만 해서 유저파일을 사용하려고 한다.
</details>

<details>
<summary><b>개발 단계</b></summary>
Python으로 프로젝트를 처음부터 짜려니 막막하다. 유지보수 하던게 다인데..
기초 문법도 다시 찾아봐야하던게 List나 Dict의 iterator가 자세히 기억이 안나고
함수 시그니처에 관해 설명을 들었을때 저런 표현방법도 있구나를 알았다. 원래는 함수
따라가면서 어떤 인자와 리턴 값이 있는지 헤딩했었는데...

그래서 이번 과제를 하면서 최대한 함수에 설명을 넣으려하였고
수업에 있었던 모듈같은 것도 분리해보려 했으며 과정에는 안나왔지만 
암호화나 실무처럼 생각나는 부분에는 try-catch를 넣어 원하는 방향으로 유도되게끔 해두었다.
하지만 그걸 우회하는 에러가 발생할지 몰라 최대한 여러번 테스트 해보고 디버깅을 다시 해봐야할 것 같다.
</details>
